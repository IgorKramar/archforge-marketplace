---
description: Generate a C4 diagram (context/container/component) — alias for `/krait_arch:diagram c4-<level>`. Kept for backward compatibility; new work can use `/krait_arch:diagram` directly with any type.
argument-hint: "<level: context|container|component> <subject>"
---

# /krait_arch:c4

This command is an alias for `/krait_arch:diagram c4-<level> <subject>`.

It generates exactly one C4 diagram. For other diagram types — sequence, state, ER, deployment — use `/krait_arch:diagram <type> <subject>` directly.

## Usage

```
/krait_arch:c4 context "the storefront and its surrounding services"
/krait_arch:c4 container "the editor backend and its dependencies"
/krait_arch:c4 component "the AI gateway internals"
```

These are equivalent to:

```
/krait_arch:diagram c4-context "the storefront and its surrounding services"
/krait_arch:diagram c4-container "the editor backend and its dependencies"
/krait_arch:diagram c4-component "the AI gateway internals"
```

## Steps

1. Parse the first argument as the C4 level (`context`, `container`, or `component`).
2. Forward to `/krait_arch:diagram` logic with type `c4-<level>`.

If the level argument is missing or invalid, ask the user to pick. Don't render multiple levels.

## Why both commands exist

`/krait_arch:c4` predates `/krait_arch:diagram`. It's kept as an alias so old workflows and muscle memory don't break. The substantive logic is in `/krait_arch:diagram` and the `architectural-diagrams` skill.

For new work — use `/krait_arch:diagram` directly. It's the single entry point for all five diagram types.
